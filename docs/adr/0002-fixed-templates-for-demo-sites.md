# Fixed templates for demo sites

Demo Sites are rendered from fixed, reviewed templates populated by structured AI content, rather than letting AI generate arbitrary page designs or HTML. This keeps layout quality predictable, reduces XSS and hallucinated-design risk, makes QA and screenshot capture easier, and supports a polished portfolio demo without giving the model control over presentation code.
