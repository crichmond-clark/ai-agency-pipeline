# Demo creation requires admin approval

Imported leads do not automatically enter demo generation. An Admin User must explicitly approve demo creation for a lead before business profile generation, demo content generation, or demo rendering begins. This keeps the pipeline human-in-the-loop from the start and prevents wasting AI work on low-quality, unsuitable, or sensitive leads.
