# Business Finder remains the lead source

Lead discovery remains in the existing `business-finder` application, and the AI demo pipeline imports leads from its CSV export for the MVP. This avoids rebuilding the Google Places scanning, website checking, and scoring workflow inside Payload while keeping the new app focused on demo generation, review, and outreach.
